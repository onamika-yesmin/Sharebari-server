# ShareBari Server

TypeScript Express backend for ShareBari.

## Scripts

```bash
npm install
npm run dev
npm run build
npm start
```

The API runs locally on `http://localhost:5000`.

## Routes

- `GET /` - server status
- `GET /api/health` - health check
